<div class="row">
    <div class="col-xs-12 col-sm-10 col-sm-offset-1 text-center">
    <% @matches.each do |mtch| %>
    <!--определяем статус матча-->
    <% if mtch[:time].is_a?(String)      
     if mtch[:time].downcase == 'live'
         color = 'PaleGreen'
       elsif mtch[:time].downcase == 'finished'
         color = 'IndianRed'
       else color = 'Beige'
     end
    else color = 'Beige'
    end %>
    
    <!--TODO: Заменить на jQ .onclick-->
    <%= link_to match_path(mtch[:m_id]) do %>
    <div class="col-md-2 text-center match">
        <div class="row match__team">
            <img  class='match__logo' src=<%= mtch[:t1_logo] %>></img><h2 class="match__organization"><%= mtch[:team1] %></h2>
        </div>
        <div class="row match__status" style="background-color:<%= color %>">
            <%= mtch[:time].is_a?(DateTime) ? mtch[:time].strftime("%H:%M") : mtch[:time] %> 
        </div>
        <div class="row match__team">
            <h2 class="match__organization"><%= mtch[:team2] %></h2> <img  class='match__logo' src=<%= mtch[:t2_logo] %>></img>
        </div>
    </div>
    <% end %>
    <% end %>
    </div>
</div>